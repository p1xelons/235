import { Application } from 'pixi.js';

// const variables, can change later for better look
const gridSize = 20;
const cellSize = 20;
const gameWidth = gridSize * cellSize;
const gameHeight = gridSize * cellSize;
const initialSpeed = 150;

// call pixiJS app
const app = new __PIXI_APP_INIT__.Application();

(async () => {
    await app.init
})